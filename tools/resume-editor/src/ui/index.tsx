/**
 * Resume Editor — UI surface. The harness (apps/web) dynamically imports this
 * module and renders exactly these two components; nothing tool-specific lives
 * in the web app.
 */
export { ToolDeskIcon } from './desk-icon'
export { ToolWorkspace } from './workspace'
